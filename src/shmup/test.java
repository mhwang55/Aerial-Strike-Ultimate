package shmup;

import java.util.ArrayList;
import java.util.Set;

public class test {
    /*
    private int Cost(ArrayList<int> X, int K) {
        /*
        if (K == 0)
        {
            int cost = 0;
            for (int i=1; i < X.size(); i++)
            {
                cost += (X.get(i) - X.get(i-1));
            }
            return cost;
        }
        else
        {
            min();
            return Cost(X, K);
        }
        int cost = 0;
        int oldCost = cost;
        for (int j=1; j < K; j++)
        {
            for (int t=j; t < X.size(); t++)
            {
                if (j == 1)
                {
                    for (int m = 1; m < t; m++)
                    {
                        cost += (X.get(m) - X.get(m - 1));
                    }
                }
                else
                {
                    int newCost = 0;
                    int s;
                    Set<int> S = min over set (j-1 <= s <= t-1);
                    for (int n = s; n < j; n++) {
                        newCost += (X.get(t) - X.get(n));
                    }
                    cost = min(oldCost, newCost);
                    oldCost = cost;
                }
            }
        }
    }
    */
}
